diff --git a/src/Util/Getopt.php b/src/Util/Getopt.php
index ba21be3fa..129f5a862 100644
--- a/src/Util/Getopt.php
+++ b/src/Util/Getopt.php
@@ -35,7 +35,7 @@ public static function getopt(array $args, $short_options, $long_options = null)
         reset($args);
         array_map('trim', $args);
 
-        while (list($i, $arg) = each($args)) {
+        foreach ($args as $i => $arg) {
             if ($arg == '') {
                 continue;
             }
